const score =400 

console.log(score); // Prints 400

const balance = new Number(300) //creating an object  Prints [Number: 300]

console.log(balance);

console.log(balance.toFixed(2)); // prints the number with upto 2 decimal places => useful for ecommerce websites

const num = 13.2134

console.log(num.toPrecision(3)); // prints the number with 3 significant digits

const money = 5000000

console.log(money.toLocaleString('en-IN')); // Inserts commas according to Indian Number System

// Maths 

console.log(Math);



